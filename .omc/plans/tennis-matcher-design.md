# 테니스 동호회 대진 생성 웹 애플리케이션 설계 문서

## 문서 정보

| 항목 | 내용 |
|------|------|
| 프로젝트명 | Tennis Matcher |
| 작성일 | 2026-01-29 |
| 버전 | 1.0 |

---

## 1. 요구사항 요약

### 1.1 핵심 요구사항

| 구분 | 내용 |
|------|------|
| 대진 형태 | 복식 위주, 인원 부족 시 단식 추가 |
| 참가자 규모 | 10-20명 소규모 |
| 수준 체계 | A/B/C/D 4등급 |
| 성별 구성 | 동성 복식 우선, 혼합 복식 후순위 |
| 대진 방식 | 리그전, 라운드 수 유동적 |
| 중복 매칭 | 가능한 피하기 |

### 1.2 운영 요구사항

| 구분 | 내용 |
|------|------|
| 사용 환경 | 반응형 웹 (모바일 + 데스크톱) |
| 데이터 관리 | 전체 이력 관리 (참가자 + 대진 기록 + 통계) |
| 운영 환경 | 클라우드 배포 |
| 인증 | 불필요 |

---

## 2. 시스템 아키텍처

### 2.1 전체 구조도

```
┌─────────────────────────────────────────────────────────────────┐
│                        클라이언트 계층                            │
│  ┌──────────────────┐              ┌──────────────────┐         │
│  │   모바일 브라우저   │              │  데스크톱 브라우저  │         │
│  └────────┬─────────┘              └────────┬─────────┘         │
│           │                                  │                   │
│           └──────────────┬───────────────────┘                   │
│                          ▼                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │              React SPA (반응형 웹 애플리케이션)              │  │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────────────┐  │  │
│  │  │참가자관리│ │이벤트관리│ │대진생성 │ │  결과/통계      │  │  │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────────────┘  │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ HTTPS (REST API)
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        서버 계층 (Vercel)                        │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                    Next.js API Routes                      │  │
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────────┐   │  │
│  │  │ Player API   │ │  Event API   │ │   Match API      │   │  │
│  │  └──────────────┘ └──────────────┘ └──────────────────┘   │  │
│  │  ┌──────────────────────────────────────────────────────┐ │  │
│  │  │           대진 생성 알고리즘 엔진                      │ │  │
│  │  │  ┌────────────┐ ┌────────────┐ ┌────────────────┐    │ │  │
│  │  │  │제약조건검사│ │ 스코어계산 │ │  최적화 탐색   │    │ │  │
│  │  │  └────────────┘ └────────────┘ └────────────────┘    │ │  │
│  │  └──────────────────────────────────────────────────────┘ │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ Prisma ORM
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      데이터베이스 계층                           │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │              PostgreSQL (Supabase/Neon)                    │  │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────────────┐  │  │
│  │  │ Player  │ │  Event  │ │  Match  │ │ MatchPlayer     │  │  │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────────────┘  │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 아키텍처 선택 근거

| 결정 | 이유 |
|------|------|
| 모노리스 (Next.js) | 소규모 프로젝트에 적합, 프론트/백엔드 통합 개발 |
| 서버리스 | 트래픽이 낮고 간헐적, 비용 효율적 |
| PostgreSQL | 관계형 데이터에 적합, 복잡한 쿼리 지원 |

---

## 3. 기술 스택

### 3.1 프론트엔드

| 기술 | 선택 | 이유 |
|------|------|------|
| 프레임워크 | **Next.js 14 (App Router)** | React 기반, SSR/SSG 지원, API Routes 내장 |
| 언어 | **TypeScript** | 타입 안정성, 개발 생산성 향상 |
| UI 라이브러리 | **shadcn/ui + Tailwind CSS** | 커스터마이징 용이, 반응형 지원 우수 |
| 상태 관리 | **TanStack Query** | 서버 상태 관리에 최적화 |
| 폼 처리 | **React Hook Form + Zod** | 타입 안전한 폼 검증 |

### 3.2 백엔드

| 기술 | 선택 | 이유 |
|------|------|------|
| 런타임 | **Node.js 20 LTS** | Next.js와 동일 생태계 |
| API | **Next.js API Routes** | 별도 서버 불필요, 배포 간소화 |
| ORM | **Prisma** | 타입 안전, 마이그레이션 자동화 |
| 검증 | **Zod** | 프론트/백엔드 스키마 공유 |

### 3.3 데이터베이스

| 기술 | 선택 | 이유 |
|------|------|------|
| DBMS | **PostgreSQL** | 관계형 데이터 모델에 적합, JSON 지원 |
| 호스팅 | **Supabase (무료 티어)** | 무료 500MB, 관리형 서비스, 백업 자동화 |
| 대안 | Neon, PlanetScale | 필요시 마이그레이션 용이 |

### 3.4 클라우드/배포

| 구분 | 선택 | 이유 |
|------|------|------|
| 호스팅 | **Vercel** | Next.js 최적화, 무료 티어 충분 |
| CI/CD | **Vercel 내장** | GitHub 연동, 자동 배포 |
| 도메인 | **Vercel 기본 도메인** | 초기에는 무료 도메인 사용 |

### 3.5 개발 도구

| 도구 | 용도 |
|------|------|
| pnpm | 패키지 관리자 (빠르고 효율적) |
| ESLint + Prettier | 코드 품질 관리 |
| Vitest | 단위 테스트 (알고리즘 검증) |
| Playwright | E2E 테스트 |

---

## 4. 데이터베이스 스키마

### 4.1 ERD (Entity Relationship Diagram)

```
┌─────────────────┐       ┌─────────────────┐
│     Player      │       │      Event      │
├─────────────────┤       ├─────────────────┤
│ id (PK)         │       │ id (PK)         │
│ name            │       │ name            │
│ gender          │       │ date            │
│ level           │       │ status          │
│ isActive        │       │ totalRounds     │
│ createdAt       │       │ createdAt       │
│ updatedAt       │       │ updatedAt       │
└────────┬────────┘       └────────┬────────┘
         │                         │
         │    ┌────────────────────┘
         │    │
         ▼    ▼
┌─────────────────────────────────────────┐
│           EventParticipant              │
├─────────────────────────────────────────┤
│ id (PK)                                 │
│ eventId (FK) ──────────────────────────►│
│ playerId (FK) ─────────────────────────►│
│ gamesPlayed                             │
│ createdAt                               │
└─────────────────────────────────────────┘
         │
         │
         ▼
┌─────────────────────────────────────────┐
│                Match                     │
├─────────────────────────────────────────┤
│ id (PK)                                 │
│ eventId (FK)                            │
│ roundNumber                             │
│ courtNumber                             │
│ matchType (SINGLES/DOUBLES)             │
│ status (SCHEDULED/IN_PROGRESS/COMPLETED)│
│ team1Score                              │
│ team2Score                              │
│ createdAt                               │
│ updatedAt                               │
└────────────────────┬────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────┐
│             MatchPlayer                  │
├─────────────────────────────────────────┤
│ id (PK)                                 │
│ matchId (FK)                            │
│ playerId (FK)                           │
│ team (1 or 2)                           │
│ createdAt                               │
└─────────────────────────────────────────┘
```

### 4.2 Prisma 스키마 정의

```prisma
// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// 참가자 (회원)
model Player {
  id        String   @id @default(cuid())
  name      String
  gender    Gender
  level     Level
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Relations
  eventParticipants EventParticipant[]
  matchPlayers      MatchPlayer[]

  @@index([gender, level])
  @@index([isActive])
}

// 이벤트 (모임/대회)
model Event {
  id          String      @id @default(cuid())
  name        String
  date        DateTime
  status      EventStatus @default(DRAFT)
  totalRounds Int         @default(0)
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt

  // Relations
  participants EventParticipant[]
  matches      Match[]

  @@index([date])
  @@index([status])
}

// 이벤트 참가자 (다대다 관계 + 추가 정보)
model EventParticipant {
  id          String   @id @default(cuid())
  eventId     String
  playerId    String
  gamesPlayed Int      @default(0)
  createdAt   DateTime @default(now())

  // Relations
  event  Event  @relation(fields: [eventId], references: [id], onDelete: Cascade)
  player Player @relation(fields: [playerId], references: [id], onDelete: Cascade)

  @@unique([eventId, playerId])
  @@index([eventId])
  @@index([playerId])
}

// 매치 (경기)
model Match {
  id          String      @id @default(cuid())
  eventId     String
  roundNumber Int
  courtNumber Int         @default(1)
  matchType   MatchType
  status      MatchStatus @default(SCHEDULED)
  team1Score  Int?
  team2Score  Int?
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt

  // Relations
  event   Event         @relation(fields: [eventId], references: [id], onDelete: Cascade)
  players MatchPlayer[]

  @@index([eventId, roundNumber])
  @@index([status])
}

// 매치 참가자 (매치-플레이어 다대다 관계)
model MatchPlayer {
  id        String   @id @default(cuid())
  matchId   String
  playerId  String
  team      Int      // 1 or 2
  createdAt DateTime @default(now())

  // Relations
  match  Match  @relation(fields: [matchId], references: [id], onDelete: Cascade)
  player Player @relation(fields: [playerId], references: [id])

  @@unique([matchId, playerId])
  @@index([matchId])
  @@index([playerId])
}

// Enums
enum Gender {
  MALE
  FEMALE
}

enum Level {
  A
  B
  C
  D
}

enum EventStatus {
  DRAFT      // 준비 중
  ACTIVE     // 진행 중
  COMPLETED  // 완료
}

enum MatchType {
  SINGLES
  DOUBLES
}

enum MatchStatus {
  SCHEDULED   // 예정
  IN_PROGRESS // 진행 중
  COMPLETED   // 완료
}
```

### 4.3 인덱스 전략

| 테이블 | 인덱스 | 용도 |
|--------|--------|------|
| Player | (gender, level) | 대진 생성 시 필터링 |
| Player | (isActive) | 활성 회원만 조회 |
| Event | (date) | 날짜별 이벤트 조회 |
| Match | (eventId, roundNumber) | 라운드별 매치 조회 |
| MatchPlayer | (playerId) | 플레이어별 매치 이력 |

---

## 5. 대진 생성 알고리즘 (핵심)

### 5.1 알고리즘 개요

**선택: 가중치 기반 탐욕 알고리즘 (Weighted Greedy Algorithm)**

| 대안 | 장단점 | 채택 여부 |
|------|--------|----------|
| 완전 탐색 | 최적해 보장, 시간 복잡도 폭발 | X |
| 유전 알고리즘 | 좋은 해, 구현 복잡 | X |
| 탐욕 알고리즘 | 빠르고 단순, 준최적해 | O |
| 제약 충족 문제(CSP) | 정확, 구현 복잡 | 향후 고려 |

**이유**: 10-20명 규모에서 탐욕 알고리즘으로도 충분히 좋은 결과를 얻을 수 있으며, 구현과 유지보수가 간단합니다.

### 5.2 제약 조건 정의

#### Hard Constraints (반드시 만족)

| 번호 | 제약 조건 | 설명 |
|------|----------|------|
| H1 | 동시 출전 불가 | 같은 라운드에 한 선수는 한 경기만 |
| H2 | 팀 구성 유효성 | 복식은 2명, 단식은 1명 |
| H3 | 최소 인원 | 경기 진행에 필요한 최소 인원 확보 |

#### Soft Constraints (최대한 만족, 가중치 적용)

| 번호 | 제약 조건 | 가중치 | 설명 |
|------|----------|--------|------|
| S1 | 수준 균형 | 30 | 같은 등급끼리 매칭 우선 |
| S2 | 성별 균형 | 25 | 동성 복식 우선 |
| S3 | 게임 수 공평 | 25 | 모든 참가자 비슷한 게임 수 |
| S4 | 상대 중복 방지 | 10 | 같은 상대와 재매칭 최소화 |
| S5 | 파트너 다양성 | 10 | 다양한 파트너와 플레이 |

### 5.3 스코어링 함수

```typescript
// 매치 품질 점수 계산 (높을수록 좋음)
function calculateMatchScore(
  team1: Player[],
  team2: Player[],
  context: MatchContext
): number {
  let score = 100; // 기본 점수

  // S1: 수준 균형 (30점)
  const levelScore = calculateLevelBalance(team1, team2);
  score += levelScore * 0.3;

  // S2: 성별 균형 (25점)
  const genderScore = calculateGenderBalance(team1, team2);
  score += genderScore * 0.25;

  // S3: 게임 수 공평 (25점)
  const gameCountScore = calculateGameCountBalance(
    [...team1, ...team2],
    context.gamesPlayedMap
  );
  score += gameCountScore * 0.25;

  // S4: 상대 중복 방지 (10점)
  const opponentScore = calculateOpponentDiversity(
    team1,
    team2,
    context.matchHistory
  );
  score += opponentScore * 0.1;

  // S5: 파트너 다양성 (10점)
  const partnerScore = calculatePartnerDiversity(
    team1,
    team2,
    context.partnerHistory
  );
  score += partnerScore * 0.1;

  return score;
}
```

### 5.4 세부 스코어 계산 로직

#### 5.4.1 수준 균형 점수

```typescript
function calculateLevelBalance(team1: Player[], team2: Player[]): number {
  const levelValues = { A: 4, B: 3, C: 2, D: 1 };

  // 팀 평균 레벨 계산
  const team1Avg = average(team1.map(p => levelValues[p.level]));
  const team2Avg = average(team2.map(p => levelValues[p.level]));

  // 레벨 차이에 따른 점수 (차이가 작을수록 높은 점수)
  const diff = Math.abs(team1Avg - team2Avg);

  if (diff === 0) return 100;      // 완벽히 균형
  if (diff <= 0.5) return 80;      // 매우 좋음
  if (diff <= 1) return 60;        // 양호
  if (diff <= 1.5) return 40;      // 보통
  if (diff <= 2) return 20;        // 불균형
  return 0;                         // 심한 불균형
}
```

#### 5.4.2 성별 균형 점수

```typescript
function calculateGenderBalance(team1: Player[], team2: Player[]): number {
  const isTeam1SameGender = team1.every(p => p.gender === team1[0].gender);
  const isTeam2SameGender = team2.every(p => p.gender === team2[0].gender);

  // 복식인 경우
  if (team1.length === 2) {
    // 둘 다 동성 복식
    if (isTeam1SameGender && isTeam2SameGender) {
      // 같은 성별끼리 대결
      if (team1[0].gender === team2[0].gender) return 100;
      // 남성팀 vs 여성팀 (허용하지만 낮은 점수)
      return 50;
    }
    // 한쪽만 혼합 복식
    if (isTeam1SameGender || isTeam2SameGender) return 60;
    // 둘 다 혼합 복식
    return 70;
  }

  // 단식인 경우
  if (team1[0].gender === team2[0].gender) return 100;
  return 30; // 이성 간 단식 (가능하면 피함)
}
```

#### 5.4.3 게임 수 공평 점수

```typescript
function calculateGameCountBalance(
  players: Player[],
  gamesPlayedMap: Map<string, number>
): number {
  const gameCounts = players.map(p => gamesPlayedMap.get(p.id) || 0);
  const avgGames = average(gameCounts);
  const minGames = Math.min(...gamesPlayedMap.values());

  // 게임 수가 적은 선수에게 보너스
  let score = 0;
  for (const count of gameCounts) {
    if (count === minGames) score += 30;
    else if (count === minGames + 1) score += 20;
    else if (count === minGames + 2) score += 10;
  }

  return Math.min(100, score);
}
```

#### 5.4.4 상대 중복 방지 점수

```typescript
function calculateOpponentDiversity(
  team1: Player[],
  team2: Player[],
  matchHistory: MatchHistory[]
): number {
  let duplicateCount = 0;

  for (const p1 of team1) {
    for (const p2 of team2) {
      const encounters = countPreviousEncounters(p1.id, p2.id, matchHistory);
      duplicateCount += encounters;
    }
  }

  // 중복이 없으면 100점, 있을수록 감점
  if (duplicateCount === 0) return 100;
  if (duplicateCount === 1) return 70;
  if (duplicateCount === 2) return 40;
  return 10;
}
```

#### 5.4.5 파트너 다양성 점수

```typescript
function calculatePartnerDiversity(
  team1: Player[],
  team2: Player[],
  partnerHistory: PartnerHistory[]
): number {
  let score = 100;

  // 복식인 경우만 적용
  if (team1.length === 2) {
    const team1PartnerCount = countPreviousPartners(
      team1[0].id,
      team1[1].id,
      partnerHistory
    );
    if (team1PartnerCount > 0) score -= 20 * team1PartnerCount;
  }

  if (team2.length === 2) {
    const team2PartnerCount = countPreviousPartners(
      team2[0].id,
      team2[1].id,
      partnerHistory
    );
    if (team2PartnerCount > 0) score -= 20 * team2PartnerCount;
  }

  return Math.max(0, score);
}
```

### 5.5 메인 알고리즘

```typescript
interface GenerateMatchesInput {
  eventId: string;
  participants: Player[];
  roundNumber: number;
  courtsAvailable: number;
  matchHistory: MatchHistory[];
  gamesPlayedMap: Map<string, number>;
}

interface GenerateMatchesOutput {
  matches: Match[];
  restingPlayers: Player[];  // 쉬는 선수들
}

function generateRoundMatches(input: GenerateMatchesInput): GenerateMatchesOutput {
  const {
    participants,
    roundNumber,
    courtsAvailable,
    matchHistory,
    gamesPlayedMap
  } = input;

  const matches: Match[] = [];
  const assignedPlayers = new Set<string>();
  const availablePlayers = [...participants];

  // 1. 게임 수가 적은 선수 우선 정렬
  availablePlayers.sort((a, b) => {
    const aGames = gamesPlayedMap.get(a.id) || 0;
    const bGames = gamesPlayedMap.get(b.id) || 0;
    return aGames - bGames;
  });

  // 2. 최대 경기 수 계산 (코트 수 고려)
  const maxMatches = courtsAvailable;
  const playersNeededForDoubles = maxMatches * 4;

  // 3. 복식/단식 결정
  let doublesCount: number;
  let singlesCount: number;

  if (availablePlayers.length >= playersNeededForDoubles) {
    // 복식만으로 충분
    doublesCount = maxMatches;
    singlesCount = 0;
  } else if (availablePlayers.length >= 4) {
    // 복식 우선, 나머지 단식
    doublesCount = Math.floor(availablePlayers.length / 4);
    const remaining = availablePlayers.length - (doublesCount * 4);
    singlesCount = Math.floor(remaining / 2);
  } else if (availablePlayers.length >= 2) {
    // 단식만
    doublesCount = 0;
    singlesCount = 1;
  } else {
    // 경기 불가
    return { matches: [], restingPlayers: availablePlayers };
  }

  // 4. 복식 경기 생성
  for (let i = 0; i < doublesCount; i++) {
    const match = createBestDoublesMatch(
      availablePlayers.filter(p => !assignedPlayers.has(p.id)),
      { matchHistory, gamesPlayedMap }
    );

    if (match) {
      matches.push({
        ...match,
        roundNumber,
        courtNumber: i + 1,
        matchType: 'DOUBLES'
      });
      match.players.forEach(p => assignedPlayers.add(p.id));
    }
  }

  // 5. 단식 경기 생성
  for (let i = 0; i < singlesCount; i++) {
    const match = createBestSinglesMatch(
      availablePlayers.filter(p => !assignedPlayers.has(p.id)),
      { matchHistory, gamesPlayedMap }
    );

    if (match) {
      matches.push({
        ...match,
        roundNumber,
        courtNumber: doublesCount + i + 1,
        matchType: 'SINGLES'
      });
      match.players.forEach(p => assignedPlayers.add(p.id));
    }
  }

  // 6. 쉬는 선수 결정
  const restingPlayers = availablePlayers.filter(
    p => !assignedPlayers.has(p.id)
  );

  return { matches, restingPlayers };
}
```

### 5.6 복식 매치 생성 (핵심 로직)

```typescript
function createBestDoublesMatch(
  availablePlayers: Player[],
  context: MatchContext
): Match | null {
  if (availablePlayers.length < 4) return null;

  let bestMatch: Match | null = null;
  let bestScore = -Infinity;

  // 가능한 모든 4명 조합 생성
  const playerCombinations = combinations(availablePlayers, 4);

  for (const fourPlayers of playerCombinations) {
    // 4명을 2:2로 나누는 모든 방법 (3가지)
    const teamDivisions = getTeamDivisions(fourPlayers);

    for (const { team1, team2 } of teamDivisions) {
      const score = calculateMatchScore(team1, team2, context);

      if (score > bestScore) {
        bestScore = score;
        bestMatch = {
          team1,
          team2,
          score,
          players: fourPlayers
        };
      }
    }
  }

  return bestMatch;
}

// 4명을 2:2로 나누는 방법 (AB vs CD, AC vs BD, AD vs BC)
function getTeamDivisions(players: Player[]): { team1: Player[]; team2: Player[] }[] {
  const [a, b, c, d] = players;
  return [
    { team1: [a, b], team2: [c, d] },
    { team1: [a, c], team2: [b, d] },
    { team1: [a, d], team2: [b, c] }
  ];
}
```

### 5.7 알고리즘 최적화

**성능 고려사항:**

| 인원 | 4명 조합 수 | 팀 분할 | 총 연산 |
|------|-------------|---------|---------|
| 10명 | 210 | x3 | 630 |
| 15명 | 1,365 | x3 | 4,095 |
| 20명 | 4,845 | x3 | 14,535 |

20명 기준 14,535번의 스코어 계산은 현대 하드웨어에서 수 밀리초 내 완료됩니다.

**추가 최적화 옵션:**
1. Early Termination: 만점(100점)에 가까운 매치 발견 시 조기 종료
2. 캐싱: 이전 라운드의 계산 결과 재사용
3. 가지치기: 명백히 나쁜 조합은 스코어 계산 생략

---

## 6. 주요 기능 및 화면 구성

### 6.1 기능 목록

| 기능 | 설명 | 우선순위 |
|------|------|----------|
| 참가자 관리 | 회원 등록/수정/삭제, 수준/성별 설정 | 높음 |
| 이벤트 관리 | 모임 생성, 참가자 선택 | 높음 |
| 대진 생성 | 알고리즘 기반 자동 대진 | 높음 |
| 라운드 추가 | 실시간 라운드 추가 생성 | 높음 |
| 결과 입력 | 경기 결과 기록 | 중간 |
| 통계 조회 | 개인/전체 통계, 승률 등 | 중간 |
| 대진표 출력 | PDF/이미지 내보내기 | 낮음 |

### 6.2 화면 구성

#### 6.2.1 메인 네비게이션

```
┌─────────────────────────────────────────────────┐
│  Tennis Matcher                                 │
├─────────────────────────────────────────────────┤
│  [참가자]  [이벤트]  [통계]                       │
└─────────────────────────────────────────────────┘
```

#### 6.2.2 참가자 관리 화면

```
┌─────────────────────────────────────────────────┐
│  참가자 관리                      [+ 새 참가자]   │
├─────────────────────────────────────────────────┤
│  검색: [____________]  필터: [전체 ▼]            │
├─────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────┐    │
│  │ 이름     │ 성별  │ 수준 │ 상태  │ 액션   │    │
│  ├──────────┼───────┼──────┼───────┼───────┤    │
│  │ 김철수   │ 남    │ A    │ 활성  │ ✏️ 🗑️ │    │
│  │ 이영희   │ 여    │ B    │ 활성  │ ✏️ 🗑️ │    │
│  │ 박민수   │ 남    │ C    │ 활성  │ ✏️ 🗑️ │    │
│  └──────────┴───────┴──────┴───────┴───────┘    │
└─────────────────────────────────────────────────┘
```

#### 6.2.3 이벤트 생성 화면

```
┌─────────────────────────────────────────────────┐
│  새 이벤트 만들기                                │
├─────────────────────────────────────────────────┤
│  이벤트명: [토요일 정기 모임      ]              │
│  날짜:     [2026-01-29          ]              │
│  코트 수:  [2 ▼]                                │
├─────────────────────────────────────────────────┤
│  참가자 선택 (12/20명 선택됨)                    │
│  ┌─────────────────────────────────────────┐    │
│  │ [✓] 김철수 (A, 남)                       │    │
│  │ [✓] 이영희 (B, 여)                       │    │
│  │ [ ] 박민수 (C, 남) - 불참                 │    │
│  │ [✓] 최지은 (A, 여)                       │    │
│  └─────────────────────────────────────────┘    │
├─────────────────────────────────────────────────┤
│           [취소]  [이벤트 시작하기]               │
└─────────────────────────────────────────────────┘
```

#### 6.2.4 대진표 화면 (핵심)

```
┌─────────────────────────────────────────────────────────────┐
│  토요일 정기 모임 (2026-01-29)                [+ 라운드 추가] │
├─────────────────────────────────────────────────────────────┤
│  Round 1                                                    │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ Court 1 (복식)                                [결과입력]│  │
│  │  김철수(A) + 이영희(B)  vs  박민수(C) + 최지은(A)        │  │
│  │                         6 : 4                          │  │
│  ├───────────────────────────────────────────────────────┤  │
│  │ Court 2 (복식)                                [결과입력]│  │
│  │  홍길동(B) + 강미나(B)  vs  정우성(C) + 한가인(B)        │  │
│  │                       진행 중                          │  │
│  └───────────────────────────────────────────────────────┘  │
│  쉬는 선수: 이수진, 김영철                                   │
├─────────────────────────────────────────────────────────────┤
│  Round 2                                                    │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ Court 1 (복식)                                        │  │
│  │  이수진(B) + 김영철(C)  vs  김철수(A) + 홍길동(B)        │  │
│  │                        예정                           │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

#### 6.2.5 통계 화면

```
┌─────────────────────────────────────────────────────────────┐
│  통계                                                       │
├─────────────────────────────────────────────────────────────┤
│  기간: [이번 달 ▼]                                          │
├─────────────────────────────────────────────────────────────┤
│  개인 통계                                                  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ 이름    │ 경기수 │ 승  │ 패  │ 승률  │ 파트너 다양성   │  │
│  ├─────────┼────────┼─────┼─────┼───────┼───────────────┤  │
│  │ 김철수  │   15   │  9  │  6  │ 60%   │ 8명           │  │
│  │ 이영희  │   14   │  8  │  6  │ 57%   │ 7명           │  │
│  └─────────┴────────┴─────┴─────┴───────┴───────────────┘  │
├─────────────────────────────────────────────────────────────┤
│  게임 수 분포                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  김철수  ████████████████ 15                          │  │
│  │  이영희  ███████████████ 14                           │  │
│  │  박민수  ██████████████ 13                            │  │
│  │  최지은  ██████████████ 13                            │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 6.3 모바일 반응형 설계

```
모바일 (< 768px)           태블릿/데스크톱 (>= 768px)
┌─────────────────┐        ┌─────────────────────────────┐
│ ≡ Tennis Matcher│        │ Tennis Matcher              │
├─────────────────┤        │ [참가자] [이벤트] [통계]      │
│                 │        ├─────────────────────────────┤
│   Round 1       │        │        2열 레이아웃          │
│ ┌─────────────┐ │        │  ┌──────────┐ ┌──────────┐  │
│ │ Court 1     │ │        │  │ Court 1  │ │ Court 2  │  │
│ │ 김철수+이영희│ │        │  │          │ │          │  │
│ │     vs      │ │        │  └──────────┘ └──────────┘  │
│ │ 박민수+최지은│ │        └─────────────────────────────┘
│ └─────────────┘ │
│ ┌─────────────┐ │
│ │ Court 2     │ │
│ │   ...       │ │
│ └─────────────┘ │
└─────────────────┘
```

---

## 7. API 설계

### 7.1 엔드포인트 목록

#### Players API

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | /api/players | 전체 참가자 목록 |
| GET | /api/players/:id | 참가자 상세 |
| POST | /api/players | 참가자 생성 |
| PUT | /api/players/:id | 참가자 수정 |
| DELETE | /api/players/:id | 참가자 삭제 |

#### Events API

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | /api/events | 이벤트 목록 |
| GET | /api/events/:id | 이벤트 상세 (매치 포함) |
| POST | /api/events | 이벤트 생성 |
| PUT | /api/events/:id | 이벤트 수정 |
| DELETE | /api/events/:id | 이벤트 삭제 |
| POST | /api/events/:id/participants | 참가자 추가 |

#### Matches API

| Method | Endpoint | 설명 |
|--------|----------|------|
| POST | /api/events/:id/generate-round | 라운드 대진 생성 |
| PUT | /api/matches/:id/score | 경기 결과 입력 |
| GET | /api/matches/:id | 경기 상세 |

#### Statistics API

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | /api/stats/players | 전체 통계 |
| GET | /api/stats/players/:id | 개인 통계 |

### 7.2 API 응답 예시

**POST /api/events/:id/generate-round**

```json
{
  "roundNumber": 1,
  "matches": [
    {
      "id": "match_123",
      "courtNumber": 1,
      "matchType": "DOUBLES",
      "team1": [
        { "id": "p1", "name": "김철수", "level": "A", "gender": "MALE" },
        { "id": "p2", "name": "이영희", "level": "B", "gender": "FEMALE" }
      ],
      "team2": [
        { "id": "p3", "name": "박민수", "level": "C", "gender": "MALE" },
        { "id": "p4", "name": "최지은", "level": "A", "gender": "FEMALE" }
      ],
      "matchScore": 85.5
    }
  ],
  "restingPlayers": [
    { "id": "p5", "name": "이수진" }
  ]
}
```

---

## 8. 프로젝트 구조

```
tennis-matcher/
├── src/
│   ├── app/                      # Next.js App Router
│   │   ├── layout.tsx            # 루트 레이아웃
│   │   ├── page.tsx              # 홈페이지
│   │   ├── players/
│   │   │   ├── page.tsx          # 참가자 목록
│   │   │   └── [id]/page.tsx     # 참가자 상세
│   │   ├── events/
│   │   │   ├── page.tsx          # 이벤트 목록
│   │   │   ├── new/page.tsx      # 이벤트 생성
│   │   │   └── [id]/page.tsx     # 이벤트 상세 (대진표)
│   │   ├── stats/
│   │   │   └── page.tsx          # 통계
│   │   └── api/                  # API Routes
│   │       ├── players/
│   │       │   ├── route.ts      # GET, POST
│   │       │   └── [id]/route.ts # GET, PUT, DELETE
│   │       ├── events/
│   │       │   ├── route.ts
│   │       │   ├── [id]/route.ts
│   │       │   └── [id]/generate-round/route.ts
│   │       ├── matches/
│   │       │   └── [id]/score/route.ts
│   │       └── stats/
│   │           └── players/route.ts
│   ├── components/
│   │   ├── ui/                   # shadcn/ui 컴포넌트
│   │   ├── players/
│   │   │   ├── PlayerForm.tsx
│   │   │   ├── PlayerList.tsx
│   │   │   └── PlayerCard.tsx
│   │   ├── events/
│   │   │   ├── EventForm.tsx
│   │   │   ├── EventList.tsx
│   │   │   └── ParticipantSelector.tsx
│   │   ├── matches/
│   │   │   ├── MatchCard.tsx
│   │   │   ├── RoundView.tsx
│   │   │   └── ScoreInput.tsx
│   │   └── stats/
│   │       ├── StatsTable.tsx
│   │       └── StatsChart.tsx
│   ├── lib/
│   │   ├── db.ts                 # Prisma 클라이언트
│   │   ├── validations.ts        # Zod 스키마
│   │   └── utils.ts              # 유틸리티 함수
│   ├── algorithm/                # 대진 생성 알고리즘
│   │   ├── index.ts              # 메인 엔트리
│   │   ├── types.ts              # 타입 정의
│   │   ├── scoring.ts            # 스코어링 함수
│   │   ├── generator.ts          # 매치 생성기
│   │   └── utils.ts              # 조합 생성 등
│   └── types/
│       └── index.ts              # 전역 타입 정의
├── prisma/
│   ├── schema.prisma             # DB 스키마
│   └── seed.ts                   # 시드 데이터
├── tests/
│   ├── algorithm/                # 알고리즘 단위 테스트
│   │   ├── scoring.test.ts
│   │   └── generator.test.ts
│   └── e2e/                      # E2E 테스트
├── public/
├── .env.example
├── package.json
├── tailwind.config.ts
├── tsconfig.json
└── README.md
```

---

## 9. 구현 단계별 계획

### Phase 1: 프로젝트 초기 설정 (1-2일)

| 태스크 | 설명 | 산출물 |
|--------|------|--------|
| 1.1 | Next.js 프로젝트 생성 | 기본 프로젝트 구조 |
| 1.2 | TypeScript, ESLint, Prettier 설정 | 개발 환경 설정 파일 |
| 1.3 | Tailwind CSS, shadcn/ui 설정 | UI 프레임워크 설정 |
| 1.4 | Prisma 초기 설정 | schema.prisma 기본 구조 |
| 1.5 | 프로젝트 구조 생성 | 폴더/파일 구조 |

**완료 기준:** `pnpm dev` 실행 시 기본 페이지 표시

### Phase 2: 데이터베이스 및 API (2-3일)

| 태스크 | 설명 | 산출물 |
|--------|------|--------|
| 2.1 | Supabase 프로젝트 생성 | DB 연결 정보 |
| 2.2 | Prisma 스키마 완성 | 전체 스키마 정의 |
| 2.3 | DB 마이그레이션 | 테이블 생성 |
| 2.4 | Players CRUD API | 참가자 API 완성 |
| 2.5 | Events CRUD API | 이벤트 API 완성 |
| 2.6 | 시드 데이터 작성 | 테스트용 샘플 데이터 |

**완료 기준:** Postman/Thunder Client로 API 테스트 성공

### Phase 3: 대진 생성 알고리즘 (3-4일)

| 태스크 | 설명 | 산출물 |
|--------|------|--------|
| 3.1 | 타입 정의 | algorithm/types.ts |
| 3.2 | 스코어링 함수 구현 | 5개 스코어링 함수 |
| 3.3 | 조합 생성 유틸리티 | combinations 함수 |
| 3.4 | 복식 매치 생성기 | createBestDoublesMatch |
| 3.5 | 단식 매치 생성기 | createBestSinglesMatch |
| 3.6 | 라운드 생성기 | generateRoundMatches |
| 3.7 | 단위 테스트 작성 | 알고리즘 검증 테스트 |

**완료 기준:** 다양한 시나리오에서 알고리즘 테스트 통과

### Phase 4: 프론트엔드 UI (4-5일)

| 태스크 | 설명 | 산출물 |
|--------|------|--------|
| 4.1 | 레이아웃 및 네비게이션 | 공통 레이아웃 |
| 4.2 | 참가자 관리 화면 | 목록, 폼, CRUD UI |
| 4.3 | 이벤트 관리 화면 | 목록, 생성, 참가자 선택 |
| 4.4 | 대진표 화면 | 라운드별 매치 표시 |
| 4.5 | 라운드 추가 기능 | 실시간 대진 생성 |
| 4.6 | 결과 입력 기능 | 스코어 입력 모달 |
| 4.7 | 반응형 스타일링 | 모바일 최적화 |

**완료 기준:** 전체 플로우 UI에서 동작 확인

### Phase 5: 통계 및 마무리 (2-3일)

| 태스크 | 설명 | 산출물 |
|--------|------|--------|
| 5.1 | 통계 API 구현 | 개인/전체 통계 API |
| 5.2 | 통계 화면 구현 | 테이블, 차트 UI |
| 5.3 | 에러 처리 | 전역 에러 핸들링 |
| 5.4 | 로딩 상태 | 스켈레톤, 스피너 |
| 5.5 | E2E 테스트 | 주요 플로우 테스트 |

**완료 기준:** 모든 기능 정상 동작

### Phase 6: 배포 (1일)

| 태스크 | 설명 | 산출물 |
|--------|------|--------|
| 6.1 | Vercel 프로젝트 설정 | 배포 환경 구성 |
| 6.2 | 환경 변수 설정 | DB 연결 등 |
| 6.3 | 도메인 연결 (선택) | 커스텀 도메인 |
| 6.4 | 배포 및 검증 | 프로덕션 테스트 |

**완료 기준:** 프로덕션 URL에서 정상 동작

---

## 10. 예상 일정 요약

| Phase | 기간 | 누적 |
|-------|------|------|
| Phase 1: 초기 설정 | 1-2일 | 2일 |
| Phase 2: DB/API | 2-3일 | 5일 |
| Phase 3: 알고리즘 | 3-4일 | 9일 |
| Phase 4: UI | 4-5일 | 14일 |
| Phase 5: 통계/마무리 | 2-3일 | 17일 |
| Phase 6: 배포 | 1일 | 18일 |

**총 예상 기간: 약 3주 (18일)**

---

## 11. 리스크 및 대응

| 리스크 | 영향 | 대응 방안 |
|--------|------|----------|
| 알고리즘 성능 | 중 | 인원 제한 (20명), 최적화 적용 |
| 동시 접속 | 낮음 | 서버리스 자동 스케일링 |
| 데이터 유실 | 중 | Supabase 자동 백업 활용 |
| 모바일 UX | 중 | 초기부터 모바일 퍼스트 설계 |

---

## 12. 성공 기준

| 기준 | 측정 방법 |
|------|----------|
| 대진 생성 시간 | 20명 기준 1초 이내 |
| 게임 수 편차 | 최대-최소 2게임 이내 |
| 중복 매칭률 | 5라운드 내 동일 상대 20% 미만 |
| 페이지 로딩 | 첫 로딩 3초 이내 |
| 모바일 사용성 | 주요 기능 터치 3번 이내 접근 |

---

## 부록: 용어 정의

| 용어 | 정의 |
|------|------|
| 라운드 | 동시에 진행되는 경기 세트 |
| 매치 | 단일 경기 (복식 또는 단식) |
| 이벤트 | 하루 모임 단위 |
| 스코어링 | 매치 품질 평가 점수 계산 |

---

**문서 끝**
